# Copy to .env. Railway: set the same variables on your web service.

DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DATABASE?sslmode=require"

# Random string, min 16 chars (used to sign session cookies). Example:
#   openssl rand -hex 32
SESSION_SECRET="replace-with-a-long-random-secret-at-least-16-chars"
